setup:
  addons:
    - plan: heroku-postgresql
 build:
  docker:
    web: ./Dockerfile
  config:
    REQUIREMENTS_FILENAME: heroku.yml
 run:
  image: web
  command:
    - echo 'Starting server after deployment!';